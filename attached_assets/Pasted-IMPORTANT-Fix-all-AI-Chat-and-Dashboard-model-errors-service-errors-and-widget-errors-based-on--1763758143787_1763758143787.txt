IMPORTANT: Fix all AI, Chat, and Dashboard model errors, service errors, and widget errors based on the error list I provide. These errors appear across the following modules:

• features/ai/models/*
• features/ai/services/*
• features/ai/screens/*
• features/ai/widgets/*
• features/chat/models/*
• features/chat/services/*
• features/chat/widgets/*
• features/dashboard/models/*

The file list contains many Dart analyzer errors including:
- expected_executable
- uri_has_not_been_generated
- missing *.g.dart files
- undefined_method for all fromJson/toJson generators
- unused imports
- deprecated members
- constructors missing key:
- private fields should be final
- not_a_type (AsyncValue.data misuse)
- undefined_named_parameter ("subtitle" not allowed)
- incomplete code at lines 1-4 of many files

YOUR JOB IS TO FIX EVERYTHING CLEANLY AND COMPLETELY.

====================================
### 🔥 PART 1 — FIX JSON SERIALIZATION SYSTEM
====================================
1. For all models in:
   features/ai/models/
   features/chat/models/
   features/dashboard/models/

Do the following:

- Add @JsonSerializable(explicitToJson: true)
- Add factory Model.fromJson + toJson
- Ensure correct part: 'xxx.g.dart';
- Ensure correct "part of" if needed
- Remove duplicate or invalid code
- Fix classes that have missing } or broken structure
- Generate correct properties with types
- Remove incomplete code at top of file
- Ensure all class definitions are fully closed

Then ensure all *.g.dart files are properly generated by:
- Making the models valid
- Using correct naming conventions

====================================
### 🔥 PART 2 — RUN BUILD RUNNER AUTOMATICALLY
====================================
After fixing all models, configure the project to generate the .g.dart files automatically.

You MUST ensure:
flutter pub run build_runner build --delete-conflicting-outputs

AND confirm the code is ready for generation.

====================================
### 🔥 PART 3 — FIX ALL "expected_executable" ERRORS
====================================
These appear at top of many files because:
- There is stray text before imports
- There is an unused comment block
- There is a merge conflict artifact
- There is an incomplete bracket or incomplete class

FIX:
- Ensure file begins with: import (...)
- Remove ANY characters before imports (even whitespace)
- Ensure no dangling braces "{}" at file start

====================================
### 🔥 PART 4 — FIX CHAT MODELS COMPLETELY
====================================
The chat models contain:
- Missing factories
- Wrong constructors
- Incomplete classes
- Missing lists
- Missing enums
- Wrong types
- Undefined fromJson methods

Ensure:
• ChatMessage
• ChatRoom
• ChatAttachment
• ChatParticipant
• WebSocketMessage
• ChatListResponse
• ChatHistoryResponse
• CreateWorkOrderChatRequest
• CreateCustomerSalesChatRequest
• CreateGroupChatRequest
• SendMessageRequest

… ALL have:
- Valid constructors
- Proper JSON serialization
- Proper default values where needed
- All fields typed properly

====================================
### 🔥 PART 5 — FIX SERVICES (ai_service.dart + chat_service.dart)
====================================

Fix:
- "expected_executable" at top
- Remove stray characters
- Replace print() with Logger
- Fix TODO comments
- Fix AsyncValue.data misuse in chat_service.dart
- Correct Riverpod usage:
  - AsyncValue<T> state
  - state = AsyncValue.data(value)

====================================
### 🔥 PART 6 — FIX ALL WIDGETS
====================================

Fix the following in all widgets:
- Add {Key? key} to constructors
- Use super.key
- Remove unused imports
- Fix deprecated withOpacity → withValues()
- Ensure build methods are correct
- Remove unused fields
- Correct "subtitle" parameter (if widget doesn't support it)

====================================
### 🔥 PART 7 — ENSURE ALL AI FEATURES ARE FULLY WIRED
====================================

Fix:
- AIChatRequest
- AISalesReplyRequest
- AIInspectionSummaryRequest
- AITechnicianHelpRequest
- AIChatResponse
- AIMessage
- AIConversation
- AIStatistics
- AIResponseFeedback
- All missing fromJson / toJson

====================================
### 🔥 PART 8 — ENSURE DASHBOARD MODELS ARE VALID
====================================

Fix:
- TimeSeriesData
- MetricCard
- EngineerRanking
- AdminMetrics
- SalesMetrics
- CustomerDashboardData
- ANY missing factory constructors
- All g.dart generation

====================================
### 🔥 PART 9 — PROJECT CLEANUP
====================================

After fixing everything:
- Remove unused imports
- Format code using:
  dart fix --apply
  dart format .

Make all code compile and run on:
✓ Android
✓ iOS
✓ Web

====================================
### FINAL REQUIREMENT
====================================
When done, ensure:
- No analyzer errors
- All *.g.dart exist
- All serialization works
- No "expected_executable" left
- No missing returns / constructors
- No dead code
- All models valid
- All chat/AI/Dashboard modules functional

Proceed and fix ALL issues now.
